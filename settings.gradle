def subprojectDirs = [
        "core",
        "gson",
        "snake-yaml",
        "toml4j",
        "typesafe-config",
        "example"
]

subprojectDirs.each {
    include it
}

rootProject.name = "SimpleConfig"
