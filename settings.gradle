def subprojectDirs = [
        ":core",
        ":example",
        ":provider:gson",
        ":provider:snake-yaml",
        ":provider:toml4j",
        ":provider:typesafe-config"
]

subprojectDirs.each {
    include it
}

rootProject.name = "SimpleConfig"
